apiVersion: apps/v1
kind: Deployment
metadata:
  name: bytebot-agent
  namespace: bytebot
  labels:
    app.kubernetes.io/name: bytebot-agent
    app.kubernetes.io/part-of: bytebot
    app.kubernetes.io/component: agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bytebot-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bytebot-agent
        app.kubernetes.io/part-of: bytebot
        app.kubernetes.io/component: agent
    spec:
      containers:
        - name: agent
          image: ghcr.io/bytebot-ai/bytebot-agent
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9991
              protocol: TCP
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: bytebot-db-url
                  key: DATABASE_URL
            - name: BYTEBOT_DESKTOP_BASE_URL
              value: "http://bytebot-desktop"
            - name: BYTEBOT_LLM_PROXY_URL
              value: "http://bytebot-llm-proxy"
          envFrom:
            - secretRef:
                name: bytebot-agent-credentials
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
