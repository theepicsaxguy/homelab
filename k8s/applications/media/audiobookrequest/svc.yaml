apiVersion: v1
kind: Service
metadata:
  name: audiobookrequest
  namespace: media
spec:
  type: ClusterIP
  selector:
    app: audiobookrequest
  ports:
    - name: http-request
      port: 8000
      targetPort: http-request
