apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: zigbee2mqtt
  name: zigbee2mqtt-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-csi-2
  resources:
    requests:
      storage: 400Mi
