apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loops-storage
  namespace: loops
  labels:
    app.kubernetes.io/name: loops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn
