apiVersion: v1
kind: Service
metadata:
  name: bytebot-llm-proxy
  namespace: bytebot
  labels:
    app.kubernetes.io/name: bytebot-llm-proxy
    app.kubernetes.io/part-of: bytebot
    app.kubernetes.io/component: proxy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bytebot-llm-proxy
  ports:
    - name: http
      port: 80
      targetPort: 4000
      protocol: TCP
