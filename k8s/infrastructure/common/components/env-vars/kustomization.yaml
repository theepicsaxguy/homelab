apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- behavior: merge
  envs:
  - base-config.yaml
  name: base-env-config

patches:
- path: resource-patch.yaml
  target:
    kind: Deployment
    labelSelector: app.kubernetes.io/part-of=infrastructure

configurations:
- varreference.yaml

vars:
- fieldref:
    fieldpath: data.ENVIRONMENT_TYPE
  name: ENVIRONMENT_TYPE
  objref:
    apiVersion: v1
    kind: ConfigMap
    name: base-env-config
- fieldref:
    fieldpath: data.LOG_LEVEL
  name: LOG_LEVEL
  objref:
    apiVersion: v1
    kind: ConfigMap
    name: base-env-config

replacements:
- source:
    fieldPath: data.ENABLE_METRICS
    kind: ConfigMap
    name: base-env-config
  targets:
  - fieldPaths:
    - spec.template.spec.containers.[name=*].env.[name=METRICS_ENABLED].value
    select:
      kind: Deployment
- source:
    fieldPath: data.ENABLE_TRACING
    kind: ConfigMap
    name: base-env-config
  targets:
  - fieldPaths:
    - spec.template.spec.containers.[name=*].env.[name=TRACING_ENABLED].value
    select:
      kind: Deployment
