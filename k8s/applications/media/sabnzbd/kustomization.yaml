apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - http-route.yaml
  - externalsecret.yaml
  - statefulset.yaml
  - svc.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: sabnzbd-init-script
    files:
      - config/configure.sh
