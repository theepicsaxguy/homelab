# k8s/infrastructure/auth/authentik/extra/blueprints/apps-immich.yaml
# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
# yamllint disable-file
# prettier-ignore-start
# eslint-disable
---
version: 1
metadata:
  name: Apps - Immich
entries:
  - id: immich_provider
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: k8s.peekoff.com/services-immich
    attrs:
      authorization_flow:
        !Find [
          authentik_flows.flow,
          [slug, "default-provider-authorization-implicit-consent"],
        ]
      signing_key:
        !Find [
          authentik_crypto.certificatekeypair,
          [name, "authentik Self-signed Certificate"],
        ]
      invalidation_flow:
        !Find [
          authentik_flows.flow,
          [slug, "default-provider-invalidation-flow"],
        ]

      client_type: confidential
      client_id: !Env IMMICH_CLIENT_ID
      client_secret: !Env IMMICH_CLIENT_SECRET
      redirect_uris:
        - url: https://photo.peekoff.com/auth/login
          matching_mode: strict
        - url: https://photo.peekoff.com/user-settings
          matching_mode: strict
        - url: app.immich:///oauth-callback
          matching_mode: strict

      access_code_validity: minutes=1
      access_token_validity: minutes=5
      refresh_token_validity: days=30

      sub_mode: hashed_user_id
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "profile"]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, "email"]]

  - id: immich_application
    model: authentik_core.application
    identifiers:
      slug: immich
    attrs:
      name: Photos
      group: Stockage
      meta_description: Gestion de photos, vidéos et albums
      icon: https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons/png/immich.png
      provider: !KeyOf immich_provider
      policy_engine_mode: any

  - id: immich_policy_binding
    model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf immich_application
      group: !Find [authentik_core.group, [name, "Immich Users"]]
    attrs:
      order: 1
