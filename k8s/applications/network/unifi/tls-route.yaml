apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: unifi-tlsroute
  namespace: unifi
spec:
  parentRefs:
    - name: tls-passthrough
      namespace: gateway
  hostnames:
    - "unifi.pc-tips.se"
  rules:
    - backendRefs:
        - name: unifi-svc
          port: 8443
