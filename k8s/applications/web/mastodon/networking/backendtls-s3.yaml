apiVersion: gateway.networking.k8s.io/v1alpha2
kind: BackendTLSPolicy
metadata:
  name: mastodon-s3-tls
  namespace: mastodon
spec:
  targetRef:
    group: ""
    kind: Service
    name: mastodon-s3-proxy
    port: 9000
