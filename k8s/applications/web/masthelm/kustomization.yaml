apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mastodonv2

helmCharts:
- name:
  releaseName:
  repo:
  valuesFile: values.yaml
  version: 4.4.3

resources:
  - namespace.yaml
  - secretstore.yaml
  - serviceaccount.yaml
  - externalsecret.yaml