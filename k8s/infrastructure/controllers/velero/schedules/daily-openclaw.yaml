apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-openclaw
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: openclaw
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - openclaw
    orLabelSelectors:
      - matchLabels:
          app.kubernetes.io/name: openclaw
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
