apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unrar-data
  namespace: unrar
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 40Gi
