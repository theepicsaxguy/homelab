apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-gpt-researcher
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: gpt-researcher
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - gpt-researcher
    orLabelSelectors:
      - matchLabels:
          app.kubernetes.io/name: gpt-researcher
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
