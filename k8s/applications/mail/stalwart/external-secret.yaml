apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: stalwart-secret
  namespace: stalwart
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-backend
    kind: ClusterSecretStore
  target:
    name: stalwart-secret
    creationPolicy: Owner
  data:
    - secretKey: ADMIN_PASSWORD
      remoteRef:
        key: stalwart-admin-password
    - secretKey: RELAY_USERNAME
      remoteRef:
        key: stalwart-relay-username
    - secretKey: RELAY_PASSWORD
      remoteRef:
        key: stalwart-relay-password
