apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-audiobookrequest
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: audiobookrequest
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - media
    orLabelSelectors:
      - matchLabels:
          app: audiobookrequest
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
