apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-mqtt
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: mqtt
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - mqtt
    orLabelSelectors:
      - matchLabels:
          app: mosquitto
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
