---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: litellm-postgresql-app-credentials
  namespace: litellm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-backend
    kind: ClusterSecretStore
  target:
    name: litellm-postgresql-app-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: postgres-litellm-username
    - secretKey: password
      remoteRef:
        key: postgres-litellm-password
