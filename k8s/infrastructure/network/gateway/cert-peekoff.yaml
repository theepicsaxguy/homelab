apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-peekoff
  namespace: gateway
spec:
  dnsNames:
    - "*.peekoff.com"
    - peekoff.com
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: cloudflare-issuer
  secretName: cert-peekoff
  usages:
    - digital signature
    - key encipherment
