apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - svc.yaml
  - http-route.yaml
  - statefulset.yaml

patches:
  - path: bazarr-s6-overlay-security-patch.yaml
    target:
      kind: StatefulSet
      name: bazarr
      namespace: media
