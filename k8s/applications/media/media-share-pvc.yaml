apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-share
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Ti
  volumeName: media-share
  storageClassName: ""
