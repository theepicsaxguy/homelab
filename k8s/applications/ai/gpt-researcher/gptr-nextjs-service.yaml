apiVersion: v1
kind: Service
metadata:
  name: gptr-nextjs
  namespace: gpt-researcher
  labels:
    app.kubernetes.io/name: gptr-nextjs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gptr-nextjs
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
