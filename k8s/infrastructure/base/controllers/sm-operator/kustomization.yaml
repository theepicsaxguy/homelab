apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sm-operator-system

resources:
  - ns.yaml
  - bitwarden-secret-external.yaml
  - auth-token-external.yaml
  - infrastructure-secrets-external.yaml

patches:
  - path: patches/configmap-patch.yaml
    target:
      kind: ConfigMap
      name: sm-operator-config-map
