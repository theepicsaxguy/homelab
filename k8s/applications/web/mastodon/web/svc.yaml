apiVersion: v1
kind: Service
metadata:
  name: mastodon-web
  namespace: mastodon
spec:
  type: ClusterIP
  selector:
    app: mastodon-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
