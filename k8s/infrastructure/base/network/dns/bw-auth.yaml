apiVersion: v1
kind: Secret
metadata:
  name: bw-auth-token
  namespace: dns
  annotations:
    bitwarden.com/sync: 'true'
    bitwarden.com/source-secret: infrastructure-secrets
type: Opaque
stringData:
  token: '{{ .token }}'
