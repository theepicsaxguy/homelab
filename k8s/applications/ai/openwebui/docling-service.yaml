apiVersion: v1
kind: Service
metadata:
  name: docling-service
  namespace: open-webui
spec:
  selector:
    app: docling
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
  type: ClusterIP