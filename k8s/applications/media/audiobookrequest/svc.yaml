apiVersion: v1
kind: Service
metadata:
  name: audiobookrequest
  namespace: audiobookshelf
spec:
  type: ClusterIP
  selector:
    app: audiobookrequest
  ports:
    - name: http-request
      port: 8000
      targetPort: http-request
