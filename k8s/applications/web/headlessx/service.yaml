apiVersion: v1
kind: Service
metadata:
  name: headlessx
  namespace: headlessx
  labels:
    app.kubernetes.io/name: headlessx
spec:
  selector:
    app.kubernetes.io/name: headlessx
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
