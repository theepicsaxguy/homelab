apiVersion: v1
kind: Service
metadata:
  name: perplexica
  namespace: perplexica
  labels:
    app.kubernetes.io/name: perplexica
    app.kubernetes.io/version: "1.11.2"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: perplexica
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
      appProtocol: http
