apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lidarr-data
  namespace: media
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
