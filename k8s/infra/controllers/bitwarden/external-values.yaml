serviceAccount:
  create: true
  name: external-secrets-sa

installCRDs: true
replicaCount: 1

webhook:
  enabled: true
  port: 9443

metrics:
  enabled: true

resources:
  requests:
    memory: '128Mi'
    cpu: '250m'
  limits:
    memory: '256Mi'
    cpu: '500m'
