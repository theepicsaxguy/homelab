gfd:
  enabled: true                 # auto-label GPU features
 timeSlicing:
   enabled: false
+nodeSelector:
+  feature.node.kubernetes.io/pci-10de.present: "true"                # set true if you need GPU sharing
