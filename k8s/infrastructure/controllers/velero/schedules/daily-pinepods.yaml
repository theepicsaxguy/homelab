apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-pinepods
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: pinepods
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - pinepods
    orLabelSelectors:
      - matchLabels:
          app.kubernetes.io/name: pinepods
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
