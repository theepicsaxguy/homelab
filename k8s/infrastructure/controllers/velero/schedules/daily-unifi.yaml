apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-unifi
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: unifi
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - unifi
    orLabelSelectors:
      - matchLabels:
          app: unifi-controller
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
