apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - http-route.yaml
  - externalsecret.yaml
  - statefulset.yaml
  - svc.yaml

configMapGenerator:
  - name: sabnzbd-seed
    options:
      disableNameSuffixHash: true
    files:
      - sabnzbd.ini.tpl

