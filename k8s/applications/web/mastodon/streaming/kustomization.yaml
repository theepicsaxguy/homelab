apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: streaming-env
    literals:
      - PORT=4000
      - STREAMING_PORT=4000
      - STREAMING_DATABASE_TIMEOUT=10

resources:
  - streaming-deployment.yaml
  - svc-streaming.yaml
