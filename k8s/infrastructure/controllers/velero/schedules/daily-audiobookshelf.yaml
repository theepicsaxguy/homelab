apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-audiobookshelf
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: audiobookshelf
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - media
    orLabelSelectors:
      - matchLabels:
          app: audiobookshelf
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
