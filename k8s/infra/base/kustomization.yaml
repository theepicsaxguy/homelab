apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - auth
  - controllers
  - crossplane-crds
  - monitoring
  - network
  - storage

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
