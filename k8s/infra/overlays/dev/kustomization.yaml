apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: dev-infra

patches:
  - path: patches/argo-rollouts.yaml
    target:
      kind: Deployment
      name: argo-rollouts

labels:
  - pairs:
      environment: development
