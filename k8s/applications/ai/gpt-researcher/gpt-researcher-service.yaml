apiVersion: v1
kind: Service
metadata:
  name: gpt-researcher
  namespace: gpt-researcher
  labels:
    app.kubernetes.io/name: gpt-researcher
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gpt-researcher
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
