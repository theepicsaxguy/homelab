apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - statefulset.yaml
  - svc.yaml
  - http-route.yaml

labels:
  - includeSelectors: true
    pairs:
      app: audiobookshelf
