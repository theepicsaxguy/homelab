apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-jellyseerr
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: jellyseerr
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - media
    orLabelSelectors:
      - matchLabels:
          app: jellyseerr
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
