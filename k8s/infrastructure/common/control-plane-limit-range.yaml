apiVersion: v1
kind: LimitRange
metadata:
  name: control-plane-limits
  namespace: kube-system
spec:
  limits:
  - default:
      cpu: 200m
      memory: 512Mi
    defaultRequest:
      cpu: 100m
      memory: 256Mi
    max:
      cpu: 1000m
      memory: 1Gi
    type: Container
