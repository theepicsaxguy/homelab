apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-babybuddy
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: babybuddy
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - babybuddy
    orLabelSelectors:
      - matchLabels:
          app: babybuddy
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
