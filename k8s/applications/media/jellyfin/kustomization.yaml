apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
- literals:
  - TZ="Europe/Stockholm"
  name: jellyfin-env

namespace: media

resources:
- svc.yaml
- http-route.yaml
- statefulset.yaml

labels:
- includeSelectors: true
  pairs:
    app: jellyfin
