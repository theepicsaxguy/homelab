apiVersion: v1
kind: LimitRange
metadata:
  name: headlessx-defaults
  namespace: headlessx
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: "1"
        memory: 1Gi
      default:
        cpu: "4"
        memory: 4Gi
