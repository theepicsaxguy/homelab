apiVersion: builtin
kind: PatchTransformer
metadata:
  name: patch-argocd-cm
target:
  kind: ConfigMap
  name: argocd-cm
patch: |-
  apiVersion: v1
  kind: ConfigMap
  metadata:
    name: argocd-cm
  data:
    accounts.kubechecks: "apiKey,login"
    kustomize.buildOptions: "--enable-helm"
