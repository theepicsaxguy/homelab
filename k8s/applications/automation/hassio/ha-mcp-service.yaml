apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: ha-mcp
    app.kubernetes.io/name: ha-mcp
  name: ha-mcp
  namespace: home-assistant
spec:
  ports:
    - name: http
      port: 8086
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: ha-mcp
    app.kubernetes.io/name: ha-mcp
  type: ClusterIP
