apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- application-set.yaml
- project.yaml
- common
- overlays
- network
- storage
- auth
- controllers
- vpn
- cluster-components
- crds

# Note: Common labels are now managed in common/kustomization.yaml
# This prevents duplication and makes the configuration more maintainable
