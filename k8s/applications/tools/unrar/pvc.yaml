apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unrar-data
  namespace: unrar
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-csi
  resources:
    requests:
      storage: 40Gi
