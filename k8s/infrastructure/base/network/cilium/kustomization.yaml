apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cilium-ca-secret.yaml
  - cilium-configmap.yaml
  - cilium-secrets-namespace.yaml
  - ns.yaml
  - cilium-daemonset.yaml

patches:
  - path: patches/daemonset-init.yaml
    target:
      kind: DaemonSet
      name: cilium
