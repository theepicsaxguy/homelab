apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: in-cluster-certs
  namespace: kubechecks
spec:
  provider:
    kubernetes:
      server:
        # No URL needed for in-cluster access
      auth:
        serviceAccount:
          name: external-secrets
          namespace: external-secrets
