apiVersion: v1
kind: Service
metadata:
  name: vllm-embed
  labels:
    app: vllm-embed
spec:
  type: ClusterIP
  selector:
    app: vllm-embed
  ports:
    - name: http
      port: 8000
      targetPort: 8000
