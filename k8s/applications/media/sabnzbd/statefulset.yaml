apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sabnzbd
  namespace: media
spec:
  serviceName: sabnzbd
  replicas: 1
  selector:
    matchLabels:
      app: sabnzbd
  volumeClaimTemplates:
    - metadata:
        name: config
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: "longhorn"
        resources:
          requests:
            storage: 5Gi
  template:
    metadata:
      labels:
        app: sabnzbd
    spec:
      securityContext:
        runAsUser: 2501
        runAsNonRoot: true
        fsGroup: 2501
      containers:
        - name: sabnzbd
          image: ghcr.io/theepicsaxguy/sabnzbd:4.5.2-6d8db60
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: sabnzbd-secrets
            - configMapRef:
                name: sabnzbd-env-config
          env:
            - name: PUID
              value: "2501"
            - name: PGID
              value: "2501"
            - name: SAB_DOWNLOAD_DIR
              value: "/downloads"
            - name: SAB_INCOMPLETE_DIR
              value: "/downloads/incomplete"
            - name: SAB_COMPLETE_DIR
              value: "/downloads/complete"
            - name: TZ
              value: "Europe/Stockholm"
          volumeMounts:
            - name: config
              mountPath: /config
            - name: downloads-root
              mountPath: /downloads
            - name: media-share
              mountPath: /downloads/complete
            - name: incomplete-downloads
              mountPath: /downloads/incomplete
            - name: tmp
              mountPath: /tmp
          livenessProbe:
            httpGet:
              path: /api?mode=version
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api?mode=version
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 3
          securityContext:
            readOnlyRootFilesystem: true
      volumes:
        - name: downloads-root
          emptyDir: {}
        - name: media-share
          persistentVolumeClaim:
            claimName: media-share
        - name: incomplete-downloads
          emptyDir: {}
        - name: tmp
          emptyDir: {}