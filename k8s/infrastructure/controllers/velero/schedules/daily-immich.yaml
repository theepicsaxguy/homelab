apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-immich
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: immich
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - immich
    orLabelSelectors:
      - matchLabels:
          app.kubernetes.io/instance: immich
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
