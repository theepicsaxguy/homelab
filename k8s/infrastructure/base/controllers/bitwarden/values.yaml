settings:
  # Configure EU region endpoints
  cloudRegion: 'EU'

  # Default refresh interval
  bwSecretsManagerRefreshInterval: '5m'

  # Default k8s cluster domain
  kubernetesClusterDomain: 'kube.pc-tips.se'

# Security settings
containers:
  enableSeccompProfileRuntimeDefault: true
