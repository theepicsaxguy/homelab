apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
- literals:
  - TZ="Europe/Oslo"
  name: common-env

# remove the explicit Namespace resource and let kustomize set it
namespace: media

resources:
- prowlarr
- radarr
- sonarr
- torrent
