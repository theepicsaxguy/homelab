apiVersion: v1
kind: Secret
metadata:
  name: bitwarden-access-token
  namespace: argocd
type: Opaque
# This secret should be created manually with:
# kubectl create secret generic bitwarden-access-token \
#   -n argocd \
#   --from-literal=token="YOUR_BITWARDEN_ACCESS_TOKEN"
data:
  # The token will be manually added, and not stored in Git for security reasons
  # token: <base64-encoded-token>
