apiVersion: v1
kind: Secret
metadata:
  name: kubechecks-vcs-token
  namespace: kubechecks
type: Opaque
# The actual token should be added via Sealed Secrets, External Secrets, or another secure method
# This template shows the required keys that should be populated
stringData:
  KUBECHECKS_VCS_TOKEN: '${GITHUB_TOKEN}'
