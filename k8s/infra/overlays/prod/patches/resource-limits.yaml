apiVersion: apps/v1
kind: Deployment
metadata:
  name: all-deployments
spec:
  template:
    spec:
      containers:
        - name: all-containers
          resources:
            requests:
              cpu: '500m'
              memory: '512Mi'
            limits:
              cpu: '4000m'
              memory: '4Gi'
