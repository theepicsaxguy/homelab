apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: mdns
  namespace: network-system
spec:
  parentRefs:
    - name: internal
      namespace: gateway
      sectionName: mdns
  rules:
    - backendRefs:
        - name: mdns-repeater
          port: 5353