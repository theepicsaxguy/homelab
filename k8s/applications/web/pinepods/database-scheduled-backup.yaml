---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pinepods-db-backup
  namespace: pinepods
spec:
  schedule: "0 0 2 * * 0"
  backupOwnerReference: self
  cluster:
    name: pinepods-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
