apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gpt-researcher

resources:
  - namespace.yaml
  - externalsecret-litellm-api-key.yaml
  - externalsecret-gpt-researcher.yaml
  - pvc.yaml
  - gpt-researcher-deployment.yaml
  - gpt-researcher-service.yaml
  - gptr-nextjs-deployment.yaml
  - gptr-nextjs-service.yaml
  - http-route.yaml
  - ws-rewriter-configmap.yaml
  - ws-rewriter-deployment.yaml

configMapGenerator:
  - name: gpt-researcher-config
    files:
      - config.json
