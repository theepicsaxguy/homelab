daemonset:
  tolerations:
    - key: "gpu"
      operator: "Equal"
      value: "true"
      effect: "NoSchedule"
gfd:
  enabled: true                 # auto-label GPU features
timeSlicing:
  enabled: false
runtimeClassName: nvidia
