apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - serviceaccount.yaml
  - rbac.yaml # Add RBAC resources
  - cronjob.yaml # Add CronJob resource

# Optional: Add namespace if not already defined elsewhere
# namespace: kube-system
