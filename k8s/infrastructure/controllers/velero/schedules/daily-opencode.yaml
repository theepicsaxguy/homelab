apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-opencode
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: opencode
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - opencode
    orLabelSelectors:
      - matchLabels:
          app: opencode-web
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
