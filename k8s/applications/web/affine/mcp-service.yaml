apiVersion: v1
kind: Service
metadata:
  name: affine-mcp
  namespace: affine
  labels:
    app.kubernetes.io/name: affine-mcp
    app.kubernetes.io/part-of: affine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: affine-mcp
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
