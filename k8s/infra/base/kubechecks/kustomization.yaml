apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubechecks

resources:
  - namespace.yaml
  - repository.yaml
  - vcs-secret.yaml
  - application.yaml

generatorOptions:
  disableNameSuffixHash: true
