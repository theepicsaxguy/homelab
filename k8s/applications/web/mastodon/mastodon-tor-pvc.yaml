apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-tor-pvc
  namespace: mastodon
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard