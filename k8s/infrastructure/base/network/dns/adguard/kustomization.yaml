apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: adguard

resources:
  - ns.yaml
  - http-route.yaml
  - secret-external.yaml
  - config-pvc.yaml

configMapGenerator:
  - name: adguardhome-config
    files:
      - AdGuardHome.yaml
