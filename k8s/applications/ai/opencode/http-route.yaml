apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: opencode-web
  namespace: opencode
spec:
  parentRefs:
    - name: internal
      namespace: gateway
  hostnames:
    - "opencode.peekoff.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/sse/stream
      backendRefs:
        - name: opencode-web
          port: 5003
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: opencode-web
          port: 5003
