apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-qdrant
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: qdrant
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - qdrant
    orLabelSelectors:
      - matchLabels:
          app: qdrant
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
