apiVersion: v1
kind: Service
metadata:
  name: moltbot
  namespace: moltbot
  labels:
    app.kubernetes.io/name: moltbot
    app.kubernetes.io/part-of: ai
spec:
  selector:
    app.kubernetes.io/name: moltbot
  ports:
    - port: 18789
      targetPort: 18789
      name: gateway
      protocol: TCP
  type: ClusterIP