apiVersion: dns.cloudflare.crossplane.io/v1alpha1
kind: Record
metadata:
  name: wildcard-cname
  namespace: cloudflare-dns
spec:
  forProvider:
    zone: pc-tips.se
    name: "*.pc-tips.se"
    type: CNAME
    value: gauss-k8s.cfargotunnel.com
    ttl: 3600
    proxied: true
  providerConfigRef:
    name: cloudflare-provider
