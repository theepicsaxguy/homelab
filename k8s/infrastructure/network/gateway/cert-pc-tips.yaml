---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-pc-tips
  namespace: gateway
spec:
  dnsNames:
    - placeholder-wildcard
    - placeholder-base
    - cilium-gateway-external.gateway.svc.placeholder-cluster
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: cloudflare-issuer
  secretName: cert-pc-tips
  usages:
    - digital signature
    - key encipherment
