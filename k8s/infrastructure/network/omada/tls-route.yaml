apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: omada-route
  namespace: omada
spec:
  parentRefs:
  - name: internal
    namespace: gateway
  hostnames: ["omada.pc-tips.se"]
  rules:
  - backendRefs:
    - name: omada-svc
      port: 8088
