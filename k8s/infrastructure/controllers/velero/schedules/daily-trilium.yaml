apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: velero-daily-trilium
  namespace: velero
  labels:
    backup-tier: daily
    backup-app: trilium
spec:
  schedule: "0 2 * * *"
  template:
    includedNamespaces:
      - trilium
    orLabelSelectors:
      - matchLabels:
          app.kubernetes.io/name: trilium
    includedResources:
      - '*'
    excludedResources:
      - events
      - storageclasses.storage.k8s.io
    defaultVolumesToFsBackup: true
    storageLocation: default
    ttl: 336h
