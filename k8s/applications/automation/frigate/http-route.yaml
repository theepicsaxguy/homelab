apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frigate-protected
  namespace: frigate
spec:
  parentRefs:
    - name: internal
      namespace: gateway
  hostnames:
    - "frigate.pc-tips.se"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: authentik-outpost
          port: 9000
