apiVersion: gateway.networking.k8s.io/v1alpha3
kind: TLSRoute
metadata:
  name: omada-route
  namespace: omada
spec:
  parentRefs:
    - name: internal
      namespace: gateway
  hostnames:
    - omada.pc-tips.se
  rules:
    - backendRefs:
        - name: omada-svc
          port: 8043
